package org.study;

public class JavaBooleans {
    public static void main(String[] args) {
        boolean flag = true;

        if (flag) { // true
            System.out.println(flag);
        }

        if (!flag) { // false
            System.out.println(flag);
        }
    }
}
